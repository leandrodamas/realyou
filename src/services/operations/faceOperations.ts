
// This file is now refactored into smaller files
// Importing from the new modular structure
import { detectAndMatchFace, registerFaceForUser } from "./face";

// Re-export the functions for backward compatibility
export { detectAndMatchFace, registerFaceForUser };
